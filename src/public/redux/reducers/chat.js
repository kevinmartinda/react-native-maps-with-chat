const initialState = {
    isLoading: true,
    isError: false,
    token: '',
    data: []
}

export default chat = (state = initialState, action) => {
    switch(action.type){
        default:
            return state
    }
}